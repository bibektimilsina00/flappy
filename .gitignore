# python
__pycache__/
*.py[cod]
.venv/
.uv/
# node
node_modules/
.next/
dist/
*.tsbuildinfo
image.png
.turbo/
# env
.env
.env.local
.env.prod
*.local
# db / celery
celerybeat-schedule*
# os
.DS_Store

# local secret backup — never commit
env_backup
*_backup

# local yt-dlp cookies — never commit
.ytdlp-cookies.txt

# Installed agent skill packs (npx skills add) — local tooling, not source
.agents/
.claude/skills/
skills-lock.json

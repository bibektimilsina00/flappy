[tool.uv.workspace]
members = ["apps/api", "apps/worker"]

[tool.ruff]
line-length = 100
target-version = "py313"

[tool.ruff.lint]
# Pinned so a ruff upgrade can't repaint the repo red.
select = ["E4", "E7", "E9", "F", "I", "UP"]
